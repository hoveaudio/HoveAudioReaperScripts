-- Toggle Repeat On Only
-- This script will turn repeat ON if it's currently off
-- If repeat is already on, it does nothing

-- Get current repeat state
local repeat_state = reaper.GetSetRepeat(-1)

-- Only turn repeat on if it's currently off (0 = off, 1 = on)
if repeat_state == 0 then
    reaper.GetSetRepeat(1)  -- Turn repeat on
end

-- No action needed if repeat is already on
